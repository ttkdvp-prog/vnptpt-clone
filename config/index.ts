/**
 * Config barrel — re-exports only. Logic stays in `lib/`.
 * Prefer importing from `@/config` for app-level configuration entry points.
 */

export * from './env';
export * from './data-source';
export * from './nav';
