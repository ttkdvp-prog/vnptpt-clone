/**
 * App Router + nav path constants for this module.
 * Keep in sync with `lib/module-nav-config.ts` and `app/(app)/…/page.tsx`.
 */
export const TEMPLATE_ROUTES = {
  list: '/he-thong/__entity__',
} as const;
