/**
 * Permission notes for this module.
 *
 * 1. Add `AppResource` + `APP_RESOURCE_TO_MODULE` in `@/lib/permissions`
 * 2. Register matrix module key in permission-modules config + seed `var_phan_quyen`
 * 3. Toolbar: `useResourcePermissions(resource)`
 * 4. Row/detail: `useCanOnRecord` when entity has `nguoi_tao`
 *
 * @see docs/patterns-permissions.md
 */
export const TEMPLATE_APP_RESOURCE = '/* TODO: AppResource key */' as const;
