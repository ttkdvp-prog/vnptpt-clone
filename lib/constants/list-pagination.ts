/** Ngưỡng bật phân trang server thay vì tải cả mảng. */
export const SERVER_PAGINATION_THRESHOLD = 500;

/** Kích thước trang mặc định khi phân trang server. */
export const SERVER_PAGE_SIZE_DEFAULT = 50;
